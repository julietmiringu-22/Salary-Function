# Salary-Function
This project involves writing a python code to import an employee's details into a single csv file from a excel sheet that contains a company's employees data. It involves utilizing importing  libraries such as pandas to read and display the data,functions to automate tedious tasks such as retrieving the employee data. 

# Visual helper

Import CSV data
Convert the data into a dictionary
Lookup function
Handle errors
Export the data
Zipped folder
Create unzipped folder with r
Display the data 

# Python 

Import the panda , os and zipfile libraries
Read the data using the .read_csv
Display the first few lines of the csv file
create the dictionary
You can list the number of employee details you would like displayed through indexing the employees name
Get employees details  and display an error if not found
Export details into csv file
Create a folder and zip it 
You can also confirm if the file exist and where it is stored

# R 

Create a directory
List the extracted file
Detect the file type and read the CSV
Display the data

#Contributor Expectations
Create a GUI to input employees details

#Known Issues
File path
file formart

